server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dngKwngg/hrm.microservices
          search-paths:
            - config
          clone-on-start: true